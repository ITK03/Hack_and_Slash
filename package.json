{
  "name": "hack-and-slash",
  "private": true,
  "scripts": {
    "serve": "python3 -m http.server 8000 -d public",
    "check": "node scripts/check-prototype.mjs && node scripts/check-daily.mjs && node scripts/check-balance.mjs && node scripts/check-consumables.mjs && node scripts/check-bundle-b.mjs",
    "check:tutorial": "node scripts/check-tutorial.mjs",
    "check:pr12": "node scripts/check-pr12-merge.mjs",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "playwright": "^1.62.1",
    "wrangler": "^4.118.0"
  }
}
