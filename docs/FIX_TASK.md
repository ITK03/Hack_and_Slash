# 差し戻し: 前回の作業を自己点検して直す（Codex 向け）

前回の作業は**指示どおりに出来ていない**という判断です。
やり直しではなく、**1項目ずつ受け入れ基準に照らして、落ちているものだけを直す**こと。

---

## 手順0（最優先・これを最初にやる）

**成果物を push すること。** 前回はローカルに commit しただけで GitHub に届いておらず、
**誰も中身を確認できませんでした**。これが最大の問題です。

```bash
git remote -v                       # origin が無ければ次を実行
git remote add origin https://github.com/ITK03/Hack_and_Slash.git
git push -u origin HEAD:refs/heads/codex-fix
```

- 認証が通らない場合は **その場で報告して止まること。** 先に進んでも成果が届かない。
- push できたら、**ブランチ名とコミットハッシュを報告に書く。**
- 以降の修正も、作業の区切りごとに push する。**溜めない。**

---

## 手順1: 受け入れ基準の自己点検

下の表を**そのまま実行して、1行ずつ ○/× を埋めて報告**すること。
「実装しました」という文章ではなく、**確かめ方と実際に出た値**を書く。
×だったものだけを直し、直したら再度この表を埋める。

点検は `scripts/game-env.mjs` の `loadGame()` でゲーム本体を読み込んで行う。
確かめ方の欄はそのまま使えるものを書いてある。

| # | 受け入れ基準 | 確かめ方 | ○/× | 実際の値 |
|---|---|---|---|---|
| 1-a | 通常攻撃の倍率が 0.68 | `CFG.ATK_MUL` | | |
| 1-b | 薙ぎ払い/突進斬り/大地砕き が通常攻撃の 1.3/1.5/1.7倍 | 装備3種（素手・深度40・深度100）で `pDmg` の期待値を出し、通常攻撃との比を取る。**3条件すべてで 1.3/1.5/1.7 になること**（装備で比が動いたら×） | | |
| 1-c | 他の攻撃技も同じ mul 方式 | `SKILL_POOL` 全体を走査し、`dmg` レンジが残っている技が0件 | | |
| 2-a | 突進斬りの発動から移動終了まで無敵 | `useSkill` で dash を撃った直後に `S.p.invulT >= S.p.dashT` | | |
| 2-b | 突進中に弾・爆発でも減らない | 突進中に `hurtP(9999)` を呼んでHPが変わらないこと | | |
| 3 | 棒立ちで毒沼を踏む | 毒沼の攻撃を発動させ、**プレイヤーを一切動かさず**に更新して HP が減ること | | |
| 4-a | 影の背後攻撃に予兆がある | 出現から爆発まで **0.3秒以上**あること | | |
| 4-b | 動けば避けられる | 出現後に離れて更新し、HPが減らないこと | | |
| 4-c | 動かなければ当たる | 出現後にその場で更新し、HPが減ること | | |
| 5 | 最上部にスタミナが無い | `#hdrStam` が DOM に無い、または最上部バーの外にある | | |
| 6-a | 最上部がプレイヤーランク | 最上部にランクとランク経験値バーが出ている | | |
| 6-b | ランクはアカウント共通 | キャラを切り替えても同じ値 | | |
| 6-c | 生還時のみ加算 | `endRun(true)` で増え、`endRun(false)` で増えないこと | | |
| 6-d | 深いほど多い | 深度10と深度100の生還で、後者が明確に多いこと | | |
| 6-e | セーブに残る | `save()` → `load()` でランクが保持されること | | |
| 7-a | 希少敵の出現率が約0.5% | 1000フロア生成して出現数を数え、**0.2〜1.0%** に入ること | | |
| 7-b | 1ダメージしか通らない | 通常攻撃・技・燃焼・毒の**すべて**で 1 しか減らないこと | | |
| 7-c | HPが深度で伸びる | 深度1で10、深度が上がるほど増えること | | |
| 7-d | 経験値が通常敵50体分 | 同深度の通常敵1体の獲得経験値 × 50 と一致すること | | |
| 8-a | 核で凸できない | `breakthroughWithCore` が存在しない、または呼ばれない | | |
| 8-b | 重複1体で1凸 | `awardCharacter` を1回呼ぶと凸が1つ上がること | | |
| 8-c | 表示が 0/1 | 限界突破画面に「核 0/80」等が出ていないこと | | |
| 9 | 装備マスに部位名が無い | 装備タブの `.slot` に「武器」「兜」等の文字が無いこと | | |
| 10 | 技一覧にタグではなく倍率 | 技一覧・技枠に `sk.tag` が出ておらず、倍率が出ていること | | |
| 11 | 技の説明が1行 | 390px幅で技の説明が折り返していないこと | | |

---

## 手順2: 検査を最後まで回す

前回「`check-balance` が14分以上返らない」として打ち切っていますが、**これは異常ではありません。**
ボス床の死亡率を別枠で測るようになったため、**今は15〜20分かかります。ハングではないので待つこと。**

```bash
node scripts/check-prototype.mjs
node scripts/check-daily.mjs
node scripts/check-progression.mjs
node scripts/check-breakthrough.mjs
node scripts/check-balance.mjs      # 15〜20分。打ち切らない
node scripts/check-consumables.mjs  # 数分
node scripts/check-bundle-b.mjs     # 10〜20分。打ち切らない
node scripts/check-elements.mjs     # 数分
```

- **時間で打ち切らないこと。** 打ち切ったものは「未確認」であって「成功」ではない。
- 途中で落ちたら、**そのエラー全文**を報告に貼る。
- Playwright が無くて `check:layout` / `check:playable` / `check:ui` が
  スキップされる場合は、**「スキップした」と明記**する。通ったことにしない。

---

## 手順3: 前回の判断を見直す

前回の報告で自分で決めた次の2点を、**測り直して再判断**すること。

1. **大地砕きの持続火力。** 「薙ぎ払いの約14%だが全周と気絶があるので変更しない」と判断しているが、
   その根拠は倍率÷再使用時間だけで、**MPを織り込んでいない**。
   薙ぎ払い 12MP/0.55秒＝21.8MP/秒、大地砕き 34MP/5.0秒＝6.8MP/秒 で、
   **どちらもMP回復（3.4/秒）を上回る**ので、実際にはMPが律速になる。
   → **MP回復を含めた実戦での毎秒ダメージ**を実測し、その値で再判断して報告する。

2. **希少敵のHPの伸び。** 深度1/25/100で 10/19/29 は、
   「深く行くほどHPが高くなる」という要求に対して**平坦すぎないか**を再検討する。
   撃破時間 4.0/7.6/11.6秒 が、その深度で立ち止まる危険に見合っているかを
   周囲の敵の攻撃力と併せて実測し、判断を報告する。

---

## 守ること（再掲・前回守られていない可能性がある）

- **外部ライブラリを足さない。** ビルド工程も増やさない。`public/index.html` 単体で動くこと。
- **検査スクリプトにゲーム本体の式・定数・確率を書き写さない。**
  必ず `public/index.html` を読み込んで、その中の関数と状態を呼んで計測する。
- **目標値・検査内容を緩めて通さない。** 通らないなら、通らなかった事実と実測値を出す。
- **判断を求められていない箇所の仕様を勝手に変えない。**
  変えたのなら、変えた事実と理由を報告に必ず書く。
- `docs/SPEC.md` を、変更内容と**理由**で更新する。

---

# 追加の依頼（自己点検が終わってから着手する）

## 依頼12: 残りの技も倍率方式へ揃え、等級で差を付ける

戦士の初期3技と同じ考え方で、**残りの技すべて**を通常攻撃比の倍率で定義する。
**等級（tier）が高いキャラの固有技ほど倍率を高くする。ただし差を大きくしすぎない。**

### 現状（測ってある）

キャラは12人、等級は N / R / SR / SSR。固有技の威力（中央値）は今こうなっている:

| 等級 | キャラ | 固有技 | 威力 | 戦士の大地砕き(91.5)比 |
|---|---|---|---|---|
| N | ガイ | 守りの構え | 0（自己強化） | — |
| R | ルカ | 疾風の一閃 | 112 | 1.22倍 |
| R | バルド | 一点突き | 180 | 1.97倍 |
| SR | ゲルト | 不落の盾 | 0（自己強化） | — |
| SR | ヴェルナ | 連斬 | 93 | 1.02倍（再使用0.45秒） |
| SSR | レオンハルト | 覇王断 | 282 | 3.09倍（再使用5.5秒） |
| N | ミラ | 水鏡 | 48 | — |
| R | ノア | 退き風 | 76 | — |
| R | イリス | 星屑 | 90 | — |
| SR | セラ | 氷牙 | 117 | — |
| SR | ファウスト | 焦土 | 136 | — |
| SSR | アルテミシア | 終焉の光 | 255 | — |

### やり方の指定

1. **倍率は「型」と「等級」の掛け算にする。**
   - 型の倍率: 技の種類（扇・突進・全周・貫通など）と**再使用時間・MP消費**から決める。
     再使用0.45秒の技と5.5秒の技に同じ倍率を付けてはいけない。
     **1秒あたりの威力**（倍率 ÷ 再使用時間）が型ごとに近い範囲へ収まるようにする。
   - 等級の倍率: **N=1.00 / R=1.06 / SR=1.12 / SSR=1.20** を推奨する。
     最上位と最下位の差が**2割**に収まる。限界突破の基礎能力補正
     （1.06/1.12/1.20）と同じ刻みなので、ゲーム内で説明が一貫する。
2. **「大きすぎる差」を作らないための上限。**
   同じ役割の技どうしで、**1秒あたりの威力が2倍を超えないこと**。
   いま覇王断が大地砕きの3.09倍あるような開きは、等級差ではなく別物になっている。
3. **自己強化系（ガイ・ゲルト）はダメージが0**なので、等級の倍率は
   **効果量（軽減率・持続・回復量）側に掛ける**。ここも上下2割に収める。
4. **等級補正が二重に効いていないか確認する。** キャラは既に等級由来の
   基礎補正（`hp:1.03` 等）を持っている。固有技にも等級倍率を掛けると二重になる。
   **合わせて何倍の開きになるかを実測し、報告すること。**

### 確認すること

- 全技（戦士・魔術師・固有）について、**1秒あたりの威力の一覧**を出す。
  外れ値（他より2倍以上）が無いこと。
- `check-balance` / `check-playable` を回し、**変更前後の到達深度**を並べる。

---

## 依頼13: 拠点から限界突破を消す

装備タブへ統合するので、**拠点タブの限界突破は削除する。**

- 該当: 拠点の「任務・育成」折りたたみの中にある、
  `${currentCharacter().n} ${lv}凸 / 5凸` と「核を投入」ボタンを出しているカード。
- 依頼8で核投入は廃止されるので、**カードごと消す**。
- 消したあと、「任務・育成」の折りたたみが**空にならないか**を確認する。
  中身が日替わり任務だけになるなら、見出しの名前を実態に合わせる。

---

## 依頼14: 装備タブの上部を作り直す

### 要求する形

```
┌─────────────────────────────────────────┐
│ ┌──────┐  [装備] [技] [能力] [限界突破]  │
│ │ アイコン │                              │
│ │  (押せる)│  レオンハルト SSR Lv60 戦闘力3,246│
│ └──────┘                                │
└─────────────────────────────────────────┘
```

- **左上にキャラクターアイコン。** その**右に4つのボタン**（装備 / 技 / 能力 / 限界突破）。
- **縦方向を使わない。** アイコンとボタンと名前を**横に並べて1つの塊**にする。
- **アイコンを押すとキャラクター一覧**（アイコンのグリッド）が開き、切り替えられる。
  それ以外の仕様はほとんど今のまま。
- **既定で「装備」が選ばれていること。**
- **4つの中身も、今よりスクロールが要らないようにまとめる。**

### いま何を置き換えるのか（測ってある）

現在は縦に3つ積んでいて、**中身が始まる前に188px**使っている:

| 要素 | 高さ |
|---|---|
| `.charBar`（編成メンバーの横並びチップ） | 65px |
| `.editCard`（編集中の枠） | 66px |
| `.innerTabs`（装備/技/能力の3ボタン） | 57px |
| **合計** | **188px** |

本文の見える高さは686pxしかないので、**上部だけで27%**を食っている。
装備画面は現状62pxぶんスクロールが要る。

**目標: 上部の塊を 100px 以下に収め、装備画面をスクロール0にする。**
達成できたかを**実測して報告すること**（`.sbody` の `scrollHeight - clientHeight` が0）。

### 作り方の指定

- `.charBar` と `.editCard` を**廃止して1つの塊に統合**する。
  編成メンバーの横並びチップは、アイコンを押して開く一覧に吸収してよい。
- 名前・等級・Lv・戦闘力は**1行**に収める。今は3行使っている。
- 4つのボタンは**アイコンの右**に置く。2×2でも横1列でもよいが、
  **押せる領域は44px以上**を守ること（この規則の検査が既にある）。
- 限界突破の中身は、依頼8のとおり「同じ仲間 0/1」の表示にする。
- **既定は装備。** `S.gearSub` の初期値と、読み込み時の既定値の両方を確認する。

### 4つの中身のコンパクト化

- **装備:** 7部位のマスは高さ90px固定を維持（縦長にすると差し戻しになった経緯がある）。
  依頼9で部位名が消えるぶん詰める。戦闘力と「おすすめを着せる」の帯も1行に。
- **技:** 依頼10・11で1行になるので、そのぶん詰まる。
- **能力:** 6項目 + 確定/やり直し。説明文を短くして行を詰める。
- **限界突破:** 段階ごとの効果表が縦に長い。畳むか、1行にまとめる。

### 確認すること

- **4画面すべてで**、390×844 でスクロール量が0（または最小）であること。実測値を出す。
- `check:ui` / `check:layout` を通すこと（枠の不揃い・はみ出し・44px・コントラストを見る）。
- キャラクター一覧から切り替えたあと、**選んでいた4つのうちどれが開くか**を決めて報告する
  （切り替え前の画面を保つのが自然だが、判断してよい）。

---

## 報告の形式（この形で出すこと）

1. **push したブランチ名とコミットハッシュ。**（無ければ、その理由）
2. **手順1の表を全行埋めたもの。** ×だった行と、直した後の値。
3. **検査8種の結果。** 打ち切ったものは「未確認」と書く。
4. **手順3の再判断**（MP込みの実戦DPS、希少敵のHPカーブ）。
5. **指示と違う実装にした箇所**があれば、その一覧と理由。
6. **依頼12**: 全技の「1秒あたりの威力」一覧と、等級補正の二重掛けの実測。
7. **依頼14**: 上部の塊の高さと、4画面それぞれのスクロール量（変更前後）。
8. 直せなかった点があれば、実測値とともに明示する。
